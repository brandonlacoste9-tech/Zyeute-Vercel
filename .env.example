# Supabase (Client-side - Production)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Supabase (Client-side - Preview Branch for main)
# These are used for preview deployments to test changes in isolation
# Create the 'dev-preview-main' branch in Supabase dashboard or via CLI:
#   supabase branches create dev-preview-main
# Then get the connection details:
#   supabase branches get dev-preview-main
VITE_SUPABASE_URL_PREVIEW=https://your-preview-project.supabase.co
VITE_SUPABASE_ANON_KEY_PREVIEW=your-preview-anon-key

# Supabase (Server-side - for webhooks and admin operations)
SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase Project Reference (for CLI operations)
# Get this from: Supabase Dashboard > Settings > General > Reference ID
SUPABASE_PROJECT_REF=your-project-ref-id

# ==================== AI SERVICES (Open Source Stack) ====================

# DeepSeek V3 - Text Generation (Ti-Guy's brain)
# Get your API key: https://platform.deepseek.com/api_keys
# Cost: ~$0.27 per 1M tokens (70x cheaper than GPT-4)
VITE_DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# Fal.ai - Image Generation (Flux.1 Schnell)
# Get your API key: https://fal.ai/dashboard/keys
# Cost: ~$0.003 per image (10x cheaper than DALL-E 3)
VITE_FAL_API_KEY=your-fal-api-key

# OpenAI (Legacy - Optional, for backwards compatibility)
# Can be removed once DeepSeek/Flux fully tested
VITE_OPENAI_API_KEY=sk-your-openai-api-key

# Stripe (Client-side)
VITE_STRIPE_PUBLIC_KEY=pk_test_your-stripe-publishable-key

# Stripe (Server-side - for webhooks)
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-signing-secret

# Optional: Google OAuth
VITE_GOOGLE_CLIENT_ID=your-google-client-id
